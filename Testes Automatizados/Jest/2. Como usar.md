## 🤖 Como usar os testes com Jest

1. **Criar o diretório `tests` dentro da pasta `main` e um arquivo para testes, EX: `calculadora`:**

**Exemplo de Arquivo de Teste (`calculadora.test.js`):**

```
const calculadora = require("../models/calculadora");

test("espero que 1 + 1 seja 2", () => {
  expect(calculadora.somar(1, 1)).toBe(2);
});

  
test("espero que 1 + 1 seja 2", () => {
  expect(calculadora.somar("teste", 1)).toBe("Erro");
});
```

2. **Criar o diretório `models` dentro da pasta `main` e um arquivo para a lógica do model, EX: `calculadora.js`:**

**Exemplo de Arquivo de Lógica (`calculadora.js`):

```

```